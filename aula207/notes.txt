# https://www.djangoproject.com

# python -m venv venv

# pip install -r requirements.txt

# django-admin startproject project .
# Cria um novo projeto chamado project
# "." e joga os arquivos na raiz do project

# python manage.py runserver
# Sobe o servidor

# Ctrl+C -> Para o servidor

# django-admin --help

# A única vez que utiliza o django-admin é para utilizar o comando
# startproject nome_do_projeto .

# Da também pra faazer django-admin runserver, porém pode ser que dê erro

# pip freeze > requirements.txt